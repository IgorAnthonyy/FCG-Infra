apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - dashboard-provider.yaml
  - configmap.yaml

configMapGenerator:
  - name: grafana-dashboards
    files:
      - UsersAPI-Dash.json